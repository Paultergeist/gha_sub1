name: CMake on multiple platforms

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  trigger_master:
    uses: Paultergeist/gha_master/.github/workflows/basic.yaml@master
      
